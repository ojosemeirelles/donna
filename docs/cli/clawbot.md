---
summary: "CLI reference for `donna clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `donna clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `donna clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `donna clawbot qr` (same behavior as [`donna qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `donna clawbot qr` -> `donna qr`
