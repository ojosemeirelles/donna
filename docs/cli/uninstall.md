---
summary: "CLI reference for `donna uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `donna uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
donna uninstall
donna uninstall --all --yes
donna uninstall --dry-run
```
