services:
  donna-gateway:
    image: ${DONNA_IMAGE:-donna:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      DONNA_GATEWAY_TOKEN: ${DONNA_GATEWAY_TOKEN}
      DONNA_ALLOW_INSECURE_PRIVATE_WS: ${DONNA_ALLOW_INSECURE_PRIVATE_WS:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${DONNA_CONFIG_DIR}:/home/node/.donna
      - ${DONNA_WORKSPACE_DIR}:/home/node/.donna/workspace
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg DONNA_INSTALL_DOCKER_CLI=1) or use
      ## docker-setup.sh with DONNA_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    ports:
      - "${DONNA_GATEWAY_PORT:-18789}:18789"
      - "${DONNA_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${DONNA_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  donna-cli:
    image: ${DONNA_IMAGE:-donna:local}
    network_mode: "service:donna-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      DONNA_GATEWAY_TOKEN: ${DONNA_GATEWAY_TOKEN}
      DONNA_ALLOW_INSECURE_PRIVATE_WS: ${DONNA_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${DONNA_CONFIG_DIR}:/home/node/.donna
      - ${DONNA_WORKSPACE_DIR}:/home/node/.donna/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - donna-gateway
